## PDF 智能分析系统（前端阶段）

本项目是一个面向 PDF 文档的智能分析系统，目标是通过 **OCR、文本摘要生成以及知识图谱构建** 等技术，为用户提供对 PDF 内容的高效理解与结构化分析。

该目录下的文件仍处于 **前端开发阶段**，后端功能（OCR / 摘要生成 / 知识图谱生成）尚未完成或尚未与前端对接。

---

## 一、当前完成进度（截至目前）

### ✅ 已完成内容（前端）

#### 1. Web 前端整体框架
- 采用 **HTML + CSS + JavaScript** 实现
- 页面结构清晰，分为：
  - 顶部系统标题栏
  - 文件上传与文件信息展示栏
  - 左侧 PDF 展示 Panel
  - 右侧分析 Panel（摘要 / 思维导图）

#### 2. PDF 浏览功能（基于 PDF.js）
已实现完整的 PDF 浏览能力，包括：
- 本地 PDF 文件上传
- PDF 页面渲染显示
- 上一页 / 下一页翻页
- PDF 缩放（放大 / 缩小）
- 固定 PDF Panel 尺寸，缩放仅影响内容大小
- 缩放后支持在 Panel 内滚动查看页面内容

#### 3. 前端交互与布局
- 左右双栏布局（PDF Panel + 分析 Panel）
- 右侧分析区支持 Tab 切换：
  - **输出（摘要）**
  - **思维导图**
- Tab 为定宽样式，符合工具型产品交互习惯
- 根据当前 Tab 显示对应的主操作按钮：
  - 输出 → 生成摘要
  - 思维导图 → 生成知识图谱
- 当前按钮仅为 UI 占位，尚未接入实际逻辑

---

## 二、尚未完成内容（计划中）

### ⏳ 后端功能（尚未对接）

以下功能已在项目中以 Python 脚本形式存在，但**尚未与 Web 前端进行接口对接**：

- OCR 服务（PDF / 图片文本识别）
- 文本摘要生成
- 知识图谱构建与可视化数据生成
- 用户管理模块（登录 / 用户状态等）

目前前端仅完成 **界面与交互层**，未进行任何网络请求或 API 调用。

---

## 三、当前项目状态说明

- 本项目当前处于 **前后端分离开发阶段**
- 前端已完成：
  - 页面结构
  - UI 布局
  - PDF 浏览基础能力
- 后端：
  - 功能脚本存在
  - 尚未封装为 Web API
  - 尚未与前端通信

---

## 四、后续开发计划（简要）

1. 将 OCR / 摘要 / 图谱相关 `.py` 脚本封装为后端服务（如 Flask / FastAPI）
2. 定义前后端接口（REST API）
3. 前端通过 `fetch / axios` 调用后端接口
4. 在前端展示 OCR、摘要和知识图谱结果
5. （可选）引入用户管理与权限控制模块

---

## 五、说明

当前提交版本主要用于展示：
- **前端架构设计**
- **PDF 浏览能力**
- **系统整体交互流程**

不代表系统最终功能完成状态。

